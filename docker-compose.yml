version: '3.8'

services:
  solar-system:
    build: .
    container_name: solar-system-3d
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
